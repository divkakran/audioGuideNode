var mongoose = require('mongoose');
var Schema = mongoose.Schema;

module.exports = mongoose.model('city' , new Schema({
    id        : String,
    city_name : String,
    monument  : [],
    language  : []
}, {collection: 'city_managements'}));
