var mongoose = require('mongoose');
var Schema = mongoose.Schema;

module.exports = mongoose.model('language' , new Schema({
    id: String,
    city_name: String,
    monument:[],
    language:[]
}, {collection: 'language_managements'}));