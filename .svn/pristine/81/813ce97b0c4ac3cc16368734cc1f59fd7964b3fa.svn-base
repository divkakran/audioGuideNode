const mongoose  = require('mongoose');
const Schema    =  mongoose.Schema;

var MarkerSchema = new Schema({
    id: String
} , {collection:'lat_long_managements'}); 

module.exports = mongoose.model('Marker' , MarkerSchema);